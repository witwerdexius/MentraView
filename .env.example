# Aus der Mentra Developer Console (console.mentraglass.com)
MENTRA_API_KEY=dein_api_key_hier

# Package-Name deiner App (muss mit dem in der Console übereinstimmen)
PACKAGE_NAME=com.beispiel.reader

# Port des Servers (Render setzt PORT automatisch)
PORT=3000
